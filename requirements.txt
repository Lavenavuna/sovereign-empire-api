# Railway Configuration (Optional)
# This file provides Railway-specific settings
# Railway works without this file, but you can use it to customize behavior

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = ""  # Leave empty - let Dockerfile CMD handle it
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Health check configuration
healthcheckPath = "/health"
healthcheckTimeout = 100
